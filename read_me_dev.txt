1. test user has the username "test_user" and password "test_password"
2. To run, go to the directory of the project, then open commandline then run "php -S localhost:8000"

/////MOVE TO final_project
